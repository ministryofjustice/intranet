apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: intranet-staging
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: intranet-staging
---
apiVersion: v1
kind: Service
metadata:
  name: pod-broadcaster-service
  labels:
    app: intranet-staging-pod-broadcaster
spec:
  ports:
  - port: 1993
    name: http
  selector:
    app: intranet-staging-pod-broadcaster
