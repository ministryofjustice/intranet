// include the toolkit (a suite of variables, classes and mixins)
@require "../../globals/css/_ietoolkit"


.c-events-item {
  oldiecf();
  &__time, &__location {
    h2 {
      width: auto;
      margin-right: 5px;
    }
    time {
      width: auto;
    }
    address {
      width: 65%;
    }
  }
  /* PARENT OVVERIDES */

  .c-events-list & {
    width: 48%;
    float:left;
    margin-right: 1%
  }
}
